# web

Phase 6. Vite + React + TypeScript, with types generated from the API's OpenAPI document.

Not scaffolded yet on purpose — there is nothing to chart until the ingest worker has
accumulated history and the Phase 4 endpoints exist to serve it.

```bash
npm create vite@latest . -- --template react-ts
```
